# Global Orchestrator dependencies.
#
# Covers backend/orchestrator/ and backend/api.py. Each agent under
# backend/agents/ has its own requirements.txt and its own virtual environment.

# --- Orchestration + LLM ---
langgraph>=0.2.60
langchain>=0.3.7
langchain-core>=0.3.15
langchain-openai>=0.2.6
openai>=1.54.0
pydantic>=2.9.0
python-dotenv>=1.0.1

# --- HTTP API (backend/api.py, served with uvicorn) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# --- Calling the agent services over HTTP (nodes/worker_node.py) ---
httpx>=0.27.0

# --- Testing ---
pytest>=8.3.2
pytest-asyncio>=0.24.0
