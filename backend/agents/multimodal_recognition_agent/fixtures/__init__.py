"""Controlled Sprint 2 fixtures.

`mock_bioclip_predictions.json` is the classification test oracle and
`mock_taxonomy.json` the mocked GBIF/NCBI records. Neither is a scientific
dataset. `make_demo_images.py` regenerates the demo images the oracle is keyed
by; no image bytes are committed.
"""
