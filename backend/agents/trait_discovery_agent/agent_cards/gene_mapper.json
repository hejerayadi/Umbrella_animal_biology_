{
  "name": "Gene Mapper",
  "type": "worker",
  "description": "Enriches a gene list received from the Genome Agent with trait-relevant Gene Ontology annotations.",
  "reports_to": "Trait Discovery Agent",
  "may_need": [
    {
      "agent": "Genome Agent",
      "note": "data dependency only: supplies gene_list upstream, orchestrator makes the cross-agent call"
    }
  ],
  "managed_agents": [],
  "managed_services": ["Gene Ontology REST API (QuickGO / AmiGO)"],
  "capabilities": [
    "GO term lookup by gene symbol",
    "Flagging genes with no GO match"
  ],
  "input": [
    { "name": "trait_name", "type": "str", "source": null },
    { "name": "gene_list", "type": "list[str]", "source": "from Genome Agent" },
    { "name": "species_name", "type": "str", "source": null }
  ],
  "output": {
    "fields": [
      { "name": "go_annotations", "type": "list[GOAnnotation]" },
      { "name": "unmatched_genes", "type": "list[str]" }
    ],
    "control_fields": ["status", "target_agent", "prompt_to_target_agent"]
  }
}