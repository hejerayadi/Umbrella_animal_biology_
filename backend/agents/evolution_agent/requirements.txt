# Standalone local dependencies for the Evolution agent
numpy==2.1.3

# --- Molecular Comparison worker (workers/molecular_comparison/logic.py) ---
# ESM-2 protein embeddings + the similarity graph. torch is imported at
# module scope, so it is a hard requirement of the whole agent, not just
# of the molecular branch.
# CPU wheels are enough — no GPU is used. To avoid pulling CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.9.1
fair-esm==2.0.0
networkx==3.4.2
requests>=2.32.0

# --- Orchestrator dependencies ---
langgraph==0.2.39
langchain-core==0.3.15
langchain-openai==0.2.4
python-dotenv==1.0.1

# --- Tracing (Sprint 4, Task 3) — was a transitive dep, pinned explicitly
# now that we import it directly for @traceable ---
langsmith==0.1.147

# --- Agent API layer (api.py) - required, do not remove ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# --- Testing ---
pytest==8.3.2
pytest-asyncio==0.24.0
