# Copy this file to .env inside biodiversity_agent/ before running this agent locally.
AGENT_NAME=biodiversity_agent
AGENT_MODE=local

# .env is git-ignored and must never be committed. This .env.example IS
# committed, so keep real secrets out of it - placeholders only.
