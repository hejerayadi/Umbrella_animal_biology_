pytest==8.3.2
pytest-asyncio==0.24.0
langgraph==0.2.39
langchain-core==0.3.15
langchain-nvidia-ai-endpoints==0.3.5
python-dotenv==1.0.1
pydantic>=2.0.0
requests>=2.28.0
# evaluation/ (Sprint 4 Part A): run_eval.py loads test_cases.yaml directly.
pyyaml>=6.0
# evaluation/ (Sprint 4 Part B, optional): trace_review.py and run_eval.py's
# LangSmith tagging both no-op cleanly if this isn't installed or
# LANGCHAIN_API_KEY isn't set, so it's not required for Part A to run.
langsmith>=0.1.0