# Copy this file to .env inside Protein_visualization/ before running this agent locally.
AGENT_NAME=Protein_visualization
AGENT_MODE=local

# .env is git-ignored and must never be committed. This .env.example IS
# committed, so keep real secrets out of it - placeholders only.
