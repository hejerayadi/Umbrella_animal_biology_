name: Protein Agent CI

on:
  pull_request:
    branches: [Group_B]
    paths:
      - "backend/Protein_visualization/**"
      - ".github/workflows/protein-agent-ci.yml"
  workflow_dispatch:

jobs:
  offline-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/Protein_visualization/requirements*.txt
      - name: Install dependencies
        working-directory: backend/Protein_visualization
        run: pip install -r requirements-dev.txt
      - name: Lint
        run: ruff check backend/Protein_visualization
      - name: Format check
        run: ruff format --check backend/Protein_visualization
      - name: Type check
        run: mypy --config-file backend/Protein_visualization/mypy.ini backend/Protein_visualization/app
      - name: Offline tests
        run: pytest backend/Protein_visualization/tests -q -m "not live and not bge and not qdrant"
